version: "3"

services:
  app:
    container_name: "fastapi-api-practice"
    build:
      
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/src
    ports:
      - 8000:8000
    env_file:
      - .env    

